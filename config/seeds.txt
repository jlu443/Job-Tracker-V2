# Hand-curated careers URLs (one per line) for the discovery step.
# Any supported ATS URL works — discover.py extracts the tenant/token/slug and
# validates it against the ATS's public API before saving:
#
#   Workday         https://{tenant}.{wd}.myworkdayjobs.com/{SiteName}
#   Greenhouse      https://boards.greenhouse.io/{token}  or  https://job-boards.greenhouse.io/{token}
#   Lever           https://jobs.lever.co/{slug}
#   Ashby           https://jobs.ashbyhq.com/{slug}
#   SmartRecruiters https://jobs.smartrecruiters.com/{Company}
#   Workable        https://apply.workable.com/{slug}
#
# Lines starting with # are ignored. Invalid or jobless boards are simply
# skipped at validation time, so it's safe to paste any careers URL here.

https://nvidia.wd5.myworkdayjobs.com/NVIDIAExternalCareerSite
https://jobs.ashbyhq.com/openai
https://jobs.smartrecruiters.com/ServiceNow
https://apply.workable.com/netguru
