# Hand-curated Workday careers URLs (one per line) for the discovery step.
# discover.py parses tenant/wd/site out of each and validates against the API.
# Add any company careers URL of the form:
#   https://{tenant}.{wd}.myworkdayjobs.com/{SiteName}
# Lines starting with # are ignored.
#
# IMPORTANT: Only add URLs that you have verified work (i.e. the full URL loads
# a real Workday careers page). The site path segment is not guessable — get it
# from the actual URL in your browser.

https://nvidia.wd5.myworkdayjobs.com/NVIDIAExternalCareerSite
