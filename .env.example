# Copy to .env for local runs (the workflow uses GitHub Secrets, not this file).
# Both are optional — the scraper runs without them, just with reduced features.

# Enables the Claude Haiku fallback for ambiguous job titles.
ANTHROPIC_API_KEY=

# Where new jobs get announced. Without it, new jobs print to stdout instead.
DISCORD_WEBHOOK_URL=
